apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper
  namespace: trackfolio
  labels: { app: scraper }
spec:
  replicas: 1
  selector:
    matchLabels: { app: scraper }
  template:
    metadata:
      labels: { app: scraper }
    spec:
      containers:
        - name: scraper
          image: trackfolio/scraper:latest
          imagePullPolicy: IfNotPresent # built into minikube's docker daemon
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef: { name: trackfolio-config }
          env:
            - name: POSTGRES_URL
              valueFrom: { secretKeyRef: { name: trackfolio-secrets, key: POSTGRES_URL } }
          # Requests are required for the HPA's CPU-utilization target to work.
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet: { path: /health, port: 8081 }
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8081 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: scraper
  namespace: trackfolio
spec:
  selector: { app: scraper }
  ports:
    - port: 8081
      targetPort: 8081
